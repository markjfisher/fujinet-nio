This folder contains the pre-framework bring up capture.
It is kept for historical reasons only.

DO NOT ADD MORE FOLDERS AND RESULTS HERE - Use the tests/feasibility/ folder for
repeatable experiments, which write their results to the build/ folder.

The reviewed C0–C9 local hardware runs are indexed in
[../bench-evidence.md](../bench-evidence.md). The raw report, trace and console
artifacts remain in each run's ignored `build/feasibility/` directory until a
future real-bus evidence package is published deliberately.
