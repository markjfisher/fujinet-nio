fujinet:
  device_name: "atari-fdrive-test"

boot:
  mode: "normal"
  config_uri: "persist:/boot/autorun.atr"
  readonly: true

wifi:
  enabled: false
  ssid: ""
  passphrase: ""

devices:
  modem:
    enabled: false
    sniffer_enabled: false
  cpm:
    enabled: false
    ccp_image: ""
  printer:
    enabled: false

netsio:
  enabled: true
  host: "localhost"
  port: __NETSIO_PORT__

clock:
  timezone: "UTC"
  enabled: true
